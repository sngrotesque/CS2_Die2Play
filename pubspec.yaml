name: cs2_die2play
description: "A new Flutter project."
publish_to: 'none'

version: 1.2.0+1

environment:
  sdk: ^3.11.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  window_manager: ^0.5.1
  file_picker: ^11.0.2
  url_launcher: ^6.3.2
  audioplayers: ^6.7.1

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/background/
    - assets/sounds/
